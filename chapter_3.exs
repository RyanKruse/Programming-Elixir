# folder -> cmd -> iex -> c "Chapter_3.exs"
# git commit -m "xyz" -> git push -u origin main
Code.compiler_options(ignore_module_conflict: true)

#                   CHAPTER 3
#________________________________________________
#
# Immutable Data
# list1 = [ 3, 2, 1 ]
# list2 = [ 4 | list1 ]

# Transforming Data
# name = "elixir"
# cap_name = String.capitalize name
# name
